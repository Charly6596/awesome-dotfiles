#
# ~/.bashrc
#

# If not running interactively, don't do anything
[[ $- != *i* ]] && return

# alias ls='ls --color=auto'
#       PS1='[\u@\h \W]\$ '

export PATH=$PATH:~/.scripts
export PATH=$PATH:/home/charly/.gem/ruby/2.6.0/bin
